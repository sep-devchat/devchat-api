#include <iostream>
#include <vector>

int main() {
    std::vector<int> values = {3, 6, 1, 8, 5, 2};
    int evens = 0;

    for (int value : values) {
        if (value % 2 == 0) {
            evens++;
        }
    }

    std::cout << "Even numbers: " << evens << '\n';
    return 0;
}
